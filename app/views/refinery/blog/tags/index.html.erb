<% content_for :body do %>
  <ul id="tags">
    <% @tags.each do |tag| %>
      <li>
        <%= link_to tag.title, refinery.blog_tag_path(tag) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
